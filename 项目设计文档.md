# 结构化日志告警聚合器

## 1. 项目目标

SLA 是面向微服务、批处理和基础设施团队的生产型结构化日志告警聚合器。它把 HTTP、Kafka、采集器或 SDK 上送的错误日志标准化，按稳定错误指纹合并成告警组，控制重复通知，并跟踪确认、恢复、解决和重开状态。系统保存原始事件、归并决策、通知尝试和状态审计，支持追溯、重放和多实例部署。

首轮工程约束：非测试 Go 文件 21–24 个，建议固定 22 个；非测试代码 2001–2199 行（测试代码不计入）。项目必须包含数据库与迁移、分层实现、HTTP API、后台任务、测试和运行校验，不按轻量 Demo 交付。

## 2. 事件模型与标准化

LogEvent 字段：event_id、occurred_at、service、environment、level、message、error_type、stack_trace、labels、request_id、source。规则：

1. service、environment、message 必填且有长度上限；错误返回字段级详情。
2. level 统一小写，默认接受 error、fatal。
3. 缺失时间使用接收时间；未来超过 5 分钟的事件按接收时间参与窗口计算。
4. 标签排序并移除 request ID、随机数、时间戳等高基数字段。
5. 消息限制 8 KB、堆栈限制 32 KB，截断写入 truncated=true。
6. event_id 唯一；重复提交返回原结果，不增加计数、不生成通知。

## 3. 指纹、相似归并与业务规则

精确指纹为以下规范化字符串的 SHA-256：

    service | environment | error_type | normalized_message | normalized_stack | selected_labels

规范化会移除 UUID、IP、数字 ID、时间戳、内存地址；路径数字替换为 :id；SQL 参数替换为问号；堆栈只保留包名、函数名和前 N 层。同一服务、环境、错误类型且处于 5 分钟窗口时，后台可计算 SimHash；相似度至少 0.92、归一化编辑距离至少 0.85 才合并。每次相似决策写入 merge_decisions，保存候选、分数、规则和时间。

策略 AlertPolicy 按“标签+错误类型、服务+环境、服务、全局默认”的顺序匹配。配置项包括 trigger_count、window_seconds、notify_after_seconds、repeat_interval_seconds、recovery_window_seconds、escalation_after_seconds、channels 和 severity。命中的策略快照写入告警组，后续配置变化不改写历史。

## 4. 告警状态机与恢复

    open -> acknowledged -> recovering -> resolved
      |         |              |
      +---------+--------------+--> suppressed

- open：达到触发阈值。
- acknowledged：人工确认处理中，仍计数并按升级策略通知。
- recovering：超过安静窗口，等待二次确认。
- resolved：连续两个扫描周期没有新事件。
- suppressed：静默期间不外发通知，但继续保存事件。

状态只能由领域服务迁移，并写入 alert_transitions。恢复 worker 每分钟扫描活动告警：首次满足窗口进入 recovering，连续两个周期后 resolved 并创建一次恢复通知；recovering 期间来新事件则原子回到原状态并增加 reopen_count。已解决告警再次出现时重新打开，保留历史 resolved_at。

## 5. 重复通知、静默与可靠性

归并事务只创建通知意图，NotificationJob 由后台 worker 投递。worker 用 PostgreSQL 租约和行级跳过锁领取任务。幂等键为 alert_id/channel/kind/sequence，成功记录外部消息 ID；失败使用指数退避，最多 8 次，最终失败可审计。手动重发必须提供原因并生成新序列号。静默规则可按告警 ID 或匹配器设置开始/结束时间，只抑制外发通知，不丢事件、不跳过状态审计。

四类后台任务：Merge（归并事件和写 outbox）、Recovery（恢复状态机）、Notification（渠道投递和重试）、Retention（归档、清租约和保留期清理）。任务支持优雅停机；毒性数据进入死信记录并触发指标。

## 6. 分层架构

    HTTP/API -> 应用服务 -> 领域模型 -> 端口接口 -> PostgreSQL/通知基础设施

目录职责：

- cmd/aggregator：启动、命令分发和依赖组装。
- internal/transport/http：路由、鉴权、DTO、游标分页和错误映射。
- internal/app：摄取、告警控制、策略管理用例。
- internal/domain：事件、指纹、策略、状态机、退避等纯规则。
- internal/port：仓储、通知、时钟和 ID 生成接口。
- internal/store/postgres：连接池、事务、事件/告警/任务持久化。
- internal/worker：归并、恢复、通知、保留任务。

应用层不直接写 SQL，所有写入在事务内完成。

## 7. 数据库与迁移

PostgreSQL 15+，时间统一 UTC。首期表：schema_migrations、log_events、alert_groups、alert_samples、alert_transitions、alert_policies、silences、notification_jobs、notification_attempts、merge_decisions、outbox_events。关键约束和索引：事件 ID 唯一、租户+指纹唯一、活动告警状态/时间索引、通知任务状态/到期索引。事件表按月分区，归档只处理已解决且超过保留期的数据。

迁移命令：

    go run ./cmd/aggregator migrate up
    go run ./cmd/aggregator migrate status
    go run ./cmd/aggregator migrate down --steps 1  # 仅开发环境

生产迁移单独执行；服务启动只校验最低迁移版本，不自动执行 destructive down migration。

## 8. HTTP API

基础路径 /v1，使用 Bearer token 鉴权，响应带 X-Request-ID。

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | /v1/events | 单条写入，返回 accepted/duplicate |
| POST | /v1/events/batch | 批量写入，最多 500 条 |
| GET | /v1/alerts | 状态、服务、环境、严重度、游标分页 |
| GET | /v1/alerts/{id} | 详情、样例、转换、通知摘要 |
| GET | /v1/alerts/{id}/events | 归并事件分页 |
| POST | /v1/alerts/{id}/ack | 确认并记录备注 |
| POST | /v1/alerts/{id}/resolve | 人工解决，可自动重开 |
| POST | /v1/alerts/{id}/silence | 创建静默 |
| DELETE | /v1/silences/{id} | 提前结束静默 |
| POST | /v1/alerts/{id}/notify | 指定渠道重发，要求 reason |
| GET/PUT | /v1/policies[/{id}] | 查询或幂等更新策略 |
| GET | /v1/healthz、/v1/readyz | 存活与就绪检查 |

错误格式：error.code、error.message、error.request_id、error.details。写入成功返回 202 Accepted，只表示已持久化并进入后台归并；重复事件返回 200 且 duplicate=true。

## 9. 安全与可观测性

配置来自环境变量和可选 YAML，环境变量优先；数据库 URL、token 哈希、worker 并发、恢复窗口、保留天数、通知超时和渠道凭证均可配置。请求体限制 1 MB，token 绑定 tenant，管理操作要求 operator 角色，日志脱敏 Authorization、token 和个人信息，Webhook 使用 HMAC 加时间戳防重放。

使用 log/slog 输出 JSON，字段含 request_id、tenant_id、alert_id、fingerprint、worker、duration_ms。指标至少包括事件接收/重复、活动告警、归并耗时、通知尝试、队列深度、恢复转换和 worker 错误。healthz 只检查进程；readyz 检查数据库、迁移版本和 outbox 写入能力。

## 10. 测试、运行与验收

测试覆盖指纹规范化、相似度阈值、状态机、退避、策略优先级、幂等、静默、重开、事务、HTTP 错误映射、租约并发、通知幂等和恢复边界。最低命令：

    go mod download
    go test ./...
    go test -race ./...
    go vet ./...
    go run ./cmd/aggregator migrate up
    go run ./cmd/aggregator serve
    curl -fsS http://127.0.0.1:8080/v1/healthz
    curl -fsS http://127.0.0.1:8080/v1/readyz

端到端必须证明：20 条相同错误只生成一个告警组和一次初始通知；停止事件后经过恢复窗口只发一次恢复通知；恢复期间再次出现事件会重开。

规模校验：

    files=$(find . -name '*.go' ! -name '*_test.go' | wc -l)
    lines=$(find . -name '*.go' ! -name '*_test.go' -print0 | xargs -0 wc -l | tail -1 | awk '{print $1}')
    test "$files" -gt 20 && test "$files" -lt 25
    test "$lines" -gt 2000 && test "$lines" -lt 2200

## 11. 文件与代码规模计划

建议固定 22 个非测试 Go 文件：入口、配置、4 个领域文件、3 个端口文件、3 个应用服务、4 个 worker、4 个 PostgreSQL 文件、2 个 HTTP 文件。实现阶段用脚本精确校准到 2001–2199 行；测试、迁移 SQL、README 和生成文件不计入。

## 12. 交付标准

交付物为 Go 模块、版本化迁移、分层实现、测试、API 示例、运行配置、健康检查、结构化日志、核心指标和规模校验脚本。验收要求：重复事件不重复计数/通知；相同指纹归并正确；失败通知可重试且可审计；静默不丢数据；恢复和重开完整；多实例不重复领取；全部测试、竞态、静态检查及规模检查通过。
